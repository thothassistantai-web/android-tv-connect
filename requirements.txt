# Android TV Connect uses system Python bindings (PyGObject via GI).
# Install OS packages instead of pip dependencies:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1 \
#     gstreamer1.0-plugins-good gstreamer1.0-pipewire adb
#
# MPRIS media controls (Cosmic/GNOME/playerctl) use session D-Bus via GIO — no extra packages.
